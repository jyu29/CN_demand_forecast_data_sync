CREATE EXTERNAL TABLE IF NOT EXISTS fcst_clean_${env}.d_business_unit (
    but_idr_business_unit    INT,
    but_num_typ_but          DECIMAL(5,0),
    but_num_business_unit    DECIMAL(9,0),
    but_sub_num_but          DECIMAL(9,0),
    reg_idr_region           SMALLINT,
    reg_lib_region           STRING,
    zod_idr_zone_dgr         SMALLINT,
    zod_lib_zone_dgr         STRING,
    fcy_idr_fiscal_comp      SMALLINT,
    fcy_lib_fiscal_comp      STRING,
    cnt_idr_country          SMALLINT,
    cnt_country_code         STRING,
    cnt_country_code_3a      STRING,
    lab_idr_label_cnt        INT,
    cur_idr_currency         SMALLINT,
    org_num_organisation_eln DECIMAL(5,0),
    niv_num_niveau_eln       DECIMAL(10,0),
    eln_num_elt_niveau_eln   DECIMAL(10,0),
    but_int_ext              STRING,
    but_code_siren           STRING,
    but_code_ident_tva       STRING,
    but_code_international   STRING,
    but_name_business_unit   STRING,
    but_short_name           STRING,
    but_requester            STRING,
    but_customer_code        STRING,
    but_closure_begin_date   TIMESTAMP,
    but_closure_end_date     TIMESTAMP,
    but_cnuf                 DECIMAL(10,0),
    but_creation_date        TIMESTAMP,
    but_update_date          TIMESTAMP,
    but_postal_addr1_label   STRING,
    but_postal_addr2_label   STRING,
    but_postcode             STRING,
    but_city                 STRING,
    but_phone                STRING,
    but_fax                  STRING,
    but_id_environnement     STRING,
    but_cif                  STRING,
    but_telex                STRING,
    but_other_address_flag   STRING,
    but_subcontractor_flag   STRING,
    but_contact              STRING,
    but_closed               STRING,
    but_international        STRING,
    but_autumn               STRING,
    but_decat_supplier       STRING,
    but_accounting_entity    STRING,
    but_jde_num              DECIMAL(15,6),
    but_cost_center          STRING,
    but_payment              STRING,
    but_activity_num         DECIMAL(10,0),
    but_store_detail         STRING,
    warehouse_type           STRING,
    but_upd_date_tec         TIMESTAMP,
    rs_technical_date        TIMESTAMP,
    rs_technical_flow        STRING
)
STORED AS PARQUET
LOCATION 's3://fcst-clean-${env}/datalake/d_business_unit/'
;